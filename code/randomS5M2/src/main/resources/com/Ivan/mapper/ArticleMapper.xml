<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Ivan.mapper.ArticleMapper">


    <!--一对多关联查询:查询所有的用户，同时还要查询出每个用户所关联的订单信息-->

    <resultMap id="articleMap" type="com.Ivan.domain.Article">
        <id property="id" column="id"></id>
        <result property="title" column="title"></result>
        <result property="content" column="content"></result>

        <!--
            collection : 一对多使用collection标签进行关联
        -->
        <collection property="commentList" ofType="com.Ivan.domain.Comment">
            <id property="id" column="cid"></id>
            <result property="content" column="content"/>
            <result property="author" column="author"/>
            <result property="a_id" column="a_id" />
        </collection>
    </resultMap>

    <resultMap id="articleCommentMap" type="com.Ivan.domain.Article">
        <id property="id" column="id"></id>
        <result property="title" column="title"></result>
        <result property="content" column="content"></result>


        <collection
                property="commentList"
                ofType="com.Ivan.domain.Comment"
                column="id"
                select="com.Ivan.mapper.CommentMapper.findByAid"
                fetchType="lazy"
        ></collection>
    </resultMap>


    <select id="displayAllArticle"  resultMap="articleMap">
        SELECT a.*,c.id cid,c.content, c.author,c.a_id FROM t_comment c RIGHT JOIN t_article a ON c.a_id = a.id
    </select>

    <select id="displayAllArticle2"  resultMap="articleCommentMap">
        select * from t_article
    </select>




</mapper>