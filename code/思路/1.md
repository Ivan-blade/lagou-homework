+ 数据库（stage6_module3）

  + 综合脚本

    ```sql
    CREATE DATABASE stage6_module3 CHARACTER SET utf8;
    
    USE stage6_module3;
    
    create table if not exists tb_dept(
    	dept_id int not null auto_increment,
      dept_name varchar(10),
      major_name varchar(10),
      telephone varchar(12),
      email varchar(20),
      primary key(dept_id)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8;
    
    
    
    Insert into tb_dept values(null,'市场部','拉勾招聘','400-400-4008','lagou@163.com');
    Insert into tb_dept values(null,'销售部','拉勾招聘','400-400-4002','laggou@163.com');
    
    create table if not exists employee(
    	emp_id int not null auto_increment,
      emp_name varchar(10),
      dept_id int,
      job_name varchar(10),
      join_date date,
      telephone varchar(11),
      primary key(emp_id)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8;
    
    insert into employee values(null,'小王',1,'客户经理','2020-06-06','13212345678');
    insert into employee values(null,'老于',1,'客户经理','2019-04-27','13512345679');
    insert into employee values(null,'老方',2,'销售经理','2015-08-14','13512345670');
    
    create table if not exists customer(
    	cus_id int not null auto_increment,
      cus_name varchar(10),
      account varchar(20),
      telphone varchar(11),
      location varchar(20),
      primary key(cus_id)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8;
    ```

    

  + 数据表

    + 部门表（tb_dept）

      + 部门号——dept_id

      + 部门名——dept_name

      + 主管——major_name

      + 联系电话——telephone

      + 邮箱号——email

      + 脚本

        ```sql
        CREATE DATABASE stage6_module3 CHARACTER SET utf8;
        
        USE stage6_module3;
        
        create table if not exists tb_dept(
        	dept_id int not null auto_increment,
          dept_name varchar(10),
          major_name varchar(10),
          telephone varchar(12),
          email varchar(20),
          primary key(dept_id)
        ) ENGINE=InnoDB DEFAULT CHARSET=utf8;
        
        
        
        Insert into tb_dept values(null,'市场部','拉勾招聘','400-400-4008','lagou@163.com');
        Insert into tb_dept values(null,'销售部','拉勾招聘','400-400-4002','laggou@163.com');
        ```

    + 员工表（employee)

      + 工号——emp_id

      + 姓名——emp_name

      + 部门号——dept_id

      + 职位——job_name

      + 入职时间——join_date

      + 联系方式——telephone

      + 脚本

        ```sql
        create table if not exists employee(
        	emp_id int not null auto_increment,
          emp_name varchar(10),
          dept_id int,
          job_name varchar(10),
          join_date date,
          telephone varchar(11),
          primary key(emp_id)
        ) ENGINE=InnoDB DEFAULT CHARSET=utf8;
        
        insert into employee values(null,'小王',1,'客户经理','2020-06-06','13212345678');
        insert into employee values(null,'老于',1,'客户经理','2019-04-27','13512345679');
        insert into employee values(null,'老方',2,'销售经理','2015-08-14','13512345670');
        ```

        

    + 客户表（customer）

      + 编号——cus_id

      + 客户名——cus_name

      + 银行账户——account

      + 电话——telephone

      + 联系地址——location

      + 脚本

        ```sql
        create table if not exists customer(
        	cus_id int not null auto_increment,
          cus_name varchar(10),
          account varchar(20),
          telphone varchar(11),
          location varchar(20),
          primary key(cus_id)
        ) ENGINE=InnoDB DEFAULT CHARSET=utf8;
        ```

        

+ 功能